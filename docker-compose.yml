services:
  backend:
    build: 
      context: ./backend
    restart: always
    networks:
      - easypanel

  frontend:
    build:
      context: ./frontend
      args:
        - VITE_API_BASE=${VITE_API_BASE:-http://localhost:3001/api}
    restart: always
    depends_on:
      - backend
    networks:
      - easypanel

networks:
  easypanel:
    external: true
